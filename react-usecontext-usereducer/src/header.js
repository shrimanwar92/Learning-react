import React from 'react';

const Header = () => {
	return(
		<div className="header-container">
            <span>Todo using useContext and useReducer</span>
        </div>
	);
};

export default Header;